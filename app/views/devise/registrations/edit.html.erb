<h2>Profil bearbeiten</h2>
<br>
<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
  <div class='px-4 py-3' >

  <div class="input-group mb-3">
    <div class="input-group-prepend">
      <span class="input-group-text" id="inputGroupFileAddon01">Avatar</span>
    </div>
    <div class="custom-file">
      <%= f.file_field :avatar, class:'custom-file-input' %>
      <label class="custom-file-label" for="inputGroupFile01">Foto wählen</label>
    </div>
  </div>


  <div class="input-group mb-3">
    <div class="input-group-prepend">
      <span class="input-group-text" id="basic-addon1">@</span>
    </div>
    <%= f.email_field :email, autocomplete: "email", class:'form-control' %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

<br>
Neues Passwort (wenn es nicht geändert werden soll, einfach leer lassen):<br>
<% if @minimum_password_length %>
  <em><%= @minimum_password_length %> Zeichen minimum</em>
<% end %><br><br>
  <div class="input-group mb-3">
    <div class="input-group-prepend">
      <span class="input-group-text" id="basic-addon1">Passwort</span>
    </div>
    <%= f.password_field :password, autocomplete: "new-password", class:'form-control' %>
   </div>


  <div class="input-group mb-3">
    <div class="input-group-prepend">
      <span class="input-group-text" id="basic-addon1">Wiederholen</span>
    </div>
    <%= f.password_field :password_confirmation, autocomplete: "new-password",class:'form-control' %>

    </div>

<br>
<p>Um Änderungen vorzunehmen, hier bitte das aktuelle Passwort eingeben: </p>
  <div class="input-group mb-3">
    <div class="input-group-prepend">
      <span class="input-group-text" id="basic-addon1">Passwort</span>
    </div>
    <%= f.password_field :current_password, autocomplete: "current-password",class:'form-control' %>
  </div>

<br>
  <div class="actions">
    <%= f.submit "Aktualisieren", class:'btn btn-primary' %>
  </div>
<% end %>
<hr>
<br>
<h3>Account löschen?</h3>

<%= button_to "Löschen", registration_path(resource_name), data: { confirm: "Sicher?" }, method: :delete, class:'btn btn-primary' %></p>

</div><br>
