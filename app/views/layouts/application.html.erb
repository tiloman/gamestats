<!DOCTYPE html>
<html>
  <head>
    <title>Gamestats</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Lighthouse Details -->
    <link rel="manifest" href="/manifest.json">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="theme-color" content="#C50001"/>
    
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag "https://www.gstatic.com/charts/loader.js", 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <%= render 'games/header' %>

    <div class='container'>
      <p class='notice'><%= notice %></p>
      <p class='alert'><%= alert %></p>
      <%= yield %>
    </div>

  </body>
</html>
